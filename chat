resource "null_resource" "promote_replica" {
  provisioner "local-exec" {
    command = "az postgres flexible-server replica promote --resource-group ${azurerm_postgresql_server.replica.resource_group_name} --name ${azurerm_postgresql_server.replica.name} --yes"
  }
  
  depends_on = [azurerm_postgresql_server.replica]
}
