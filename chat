@pytest.mark.asyncio
async def test_validate_transaction_success(session, transaction_schema):
    # The session here is the one created by the fixture
    result = await validate_transaction(transaction_schema, session)
    assert result["message"] == "Validation successful"
    assert result["status"] == 200
